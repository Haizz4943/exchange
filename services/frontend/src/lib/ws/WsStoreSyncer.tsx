'use client';

/**
 * WsStoreSyncer — the real implementation lives in src/panel/WsStoreSyncer.tsx.
 * It is placed there (not here) because lib/ cannot import from features/.
 * Use: import { WsStoreSyncer } from '@/panel/WsStoreSyncer'
 *
 * This file is a placeholder to keep the import path documented in §5.5.
 */
export {};
