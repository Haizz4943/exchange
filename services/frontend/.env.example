# Gateway base URL — the single entry point for all API calls
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080

# Default trading pair shown on load
NEXT_PUBLIC_DEFAULT_PAIR=BTCUSDT

# Comma-separated list of supported trading pairs
NEXT_PUBLIC_SUPPORTED_PAIRS=BTCUSDT,ETHUSDT,BNBUSDT,SOLUSDT,XRPUSDT
